create database Senales;
use Senales;
create table Senal(
`id` INT NOT NULL,
`Ancho de banda` INT not null,
`Potencia pico` INT not null,
`Temp` INT not null,
primary key (`id`)
);

ALTER TABLE `Senal`
  MODIFY `id` int NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=15;
