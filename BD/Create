create database Senales;
use Senales;
create table Senal(
`id` INT NOT NULL,
`Frecuencia_Central` INT NOT NULL,
`Ancho_de_banda` INT not null,
`Potencia_pico` INT not null,
`Temp` INT not null,
primary key (`id`)
);

ALTER TABLE `Senal`
  MODIFY `id` int NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=0;
